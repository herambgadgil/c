#include <stdio.h>

// declare variables
int myFirstVariable = 1;
char myFirstCharacter = 'H';
float myFirstFloat = 14.99;

// build a function to output
int main(){
    printf("\n\n%c is the Element with atomic number %d and atomic weight %f", myFirstCharacter, myFirstVariable, myFirstFloat);
    return 0;
}
